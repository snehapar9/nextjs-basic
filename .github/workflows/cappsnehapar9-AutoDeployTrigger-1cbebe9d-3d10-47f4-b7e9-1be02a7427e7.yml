name: Trigger auto deployment for cappsnehapar9

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/cappsnehapar9-AutoDeployTrigger-1cbebe9d-3d10-47f4-b7e9-1be02a7427e7.yml'

  # Allow manual trigger 
  workflow_dispatch:
      
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: ${{ secrets.CAPPSNEHAPAR9_AZURE_CREDENTIALS }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CAPPSNEHAPAR9_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CAPPSNEHAPAR9_REGISTRY_PASSWORD }}
          containerAppName: cappsnehapar9
          resourceGroup: ca-snehapar-group
          imageToBuild: default/buildname:${{ github.sha }}
          

     

